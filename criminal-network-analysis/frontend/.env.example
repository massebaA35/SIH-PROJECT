# Copy to .env.local and adjust if your backend runs on a different host/port.
# Port 8010 (not the more common 8000) is used by default because this project
# lives alongside another local prototype that already occupies :8000 -- pick
# whichever free port you actually start the backend on.
VITE_API_BASE_URL=http://localhost:8010/api
